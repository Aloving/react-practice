// TODO: реализовать виртуальный список
// Рендерить только видимые элементы + буфер, остальное — placeholder по высоте.

export function VirtualList() {
  return (
    <div>
      <p>Компонент виртуального списка — реализуй по условию в README.</p>
    </div>
  )
}
